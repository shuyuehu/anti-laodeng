{
  "type": "object",
  "properties": {
    "risk_level": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "scene": {
      "type": "string",
      "description": "推断的职场场景，如催进度、绩效谈话、会议施压等"
    },
    "rewrite_mild": {
      "type": "string",
      "description": "温和版改写，2-4句中文，可直接发送"
    },
    "rewrite_balanced": {
      "type": "string",
      "description": "平衡版改写，2-4句中文，可直接发送"
    },
    "rewrite_direct": {
      "type": "string",
      "description": "直接版改写，2-4句中文，可直接发送"
    },
    "problem": {
      "type": "string",
      "description": "登味来源的简要分析，2-3句中文。说明哪些用词有登味、对方会有什么感受。"
    },
    "follow_up": {
      "type": "string",
      "description": "后续建议，1-2句中文。如无必要可为空字符串"
    }
  },
  "required": [
    "risk_level",
    "scene",
    "rewrite_mild",
    "rewrite_balanced",
    "rewrite_direct",
    "problem",
    "follow_up"
  ],
  "additionalProperties": false
}
