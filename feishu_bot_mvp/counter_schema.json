{
  "type": "object",
  "properties": {
    "risk_level": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "reply_mild": {
      "type": "string",
      "description": "温和版回复，1-3句中文，可直接发送"
    },
    "reply_balanced": {
      "type": "string",
      "description": "平衡版回复，1-3句中文，可直接发送"
    },
    "reply_direct": {
      "type": "string",
      "description": "反击版回复，1-3句中文，可直接发送"
    },
    "analysis": {
      "type": "string",
      "description": "简要分析，2-3句中文。说明场景、对方的施压策略、用户的即时目标。"
    },
    "follow_up": {
      "type": "string",
      "description": "后续建议，1句中文。如无必要可为空字符串"
    }
  },
  "required": [
    "risk_level",
    "reply_mild",
    "reply_balanced",
    "reply_direct",
    "analysis",
    "follow_up"
  ],
  "additionalProperties": false
}
